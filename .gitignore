# Large merged dataset outputs — regenerable locally via merge_dataset.py
*.jsonl.gz

# Local scrape working dirs
pipeline_output/
extra_channels_output/
masterclass_local/

# Local-only: large video lessons (700MB+, exceeds GitHub file size limits)
creator-rant/

# Python bytecode
__pycache__/
*.pyc

# Environment variables — never commit real keys
.env
.env.local

# Scrape outputs (large, regenerable)
checkpoint/
checkpoints_api/
raw_data/
everything_raw.jsonl
everything_training.jsonl
api_raw.jsonl
api_training.jsonl
video_ids_cache.json
video_ids_cache_api.json
yt_transcripts/

# Runtime state (regenerated each run)
quota_state.json
